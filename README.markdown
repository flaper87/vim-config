# This is how I like my vim.

## Installation procedure

### Unix like systems

    $ cd ~/
    $ git clone git://github.com/futurecat/vim-config.git 
    $ ln -s ~/vim-config/_vimrc ~/.vimrc
    $ ln -s ~/vim-config/vim ~/.vim
